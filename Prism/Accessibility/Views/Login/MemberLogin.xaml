<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
    ios:Page.UseSafeArea="True"
    x:Class="Accessibility.Views.Login.MemberLogin">
    <ContentPage.Content>
        <StackLayout
            Margin="10">
            <Label
                Text="Member Login Page: Page 3" />
            <Button
                Text="Move To Member Dashboard Tab Navigation (broken)"
                Command="{Binding ButtonCommand1}" />
            <Button
                Text="Move To Member Dashboard (work around)"
                Command="{Binding ButtonCommand2}" />
            <Button
                Text="Move To Member Dashboard with root (broken)"
                Command="{Binding ButtonCommand3}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>